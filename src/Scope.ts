export enum Scope {

    /** Access to a user's World of Warcraft characters. */
    WOW_PROFILE = "wow.profile",

    /** Access to a user's StarCraft II profile. */
    SC2_PROFILE = "sc2.profile",

    /** Access to a user's Diablo III profile. */
    D3_PROFILE = "d3.profile",

    /** Access details about a currently logged in user using OpenID Connect (OIDC). */
    OPENID = "openid"

}
